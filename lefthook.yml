pre-commit:
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --fix
      stage_fixed: true

pre-push:
  commands:
    test:
      glob: "*.go"
      run: go test ./internal/... -short -count=1
    vet-e2e:
      glob: "*.go"
      run: go vet -tags=e2e ./test/e2e/...
